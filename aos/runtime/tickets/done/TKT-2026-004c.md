# TKT-2026-004c — 撤回 004b 的归一方向：strategy-researcher → strategy_researcher

- **id**: TKT-2026-004c
- **title**: 技能目录命名纠偏，回归下划线惯例
- **intent_type**: infrastructure / correction
- **assignee**: openclaw-agent (self-executed)
- **created_at**: 2026-05-07
- **due**: 2026-05-08
- **priority**: P1（阻塞 TKT-2026-005）
- **status**: open
- **supersedes**: TKT-2026-004b（方向性纠偏，非回滚 git）

## 1. 背景 / 诚实复盘

TKT-2026-004b 在缺乏对 `~/.openclaw/workspace/skills/` 下既有目录命名形态的核实下，错误地把 strategy_researcher 归一为 strategy-researcher。Boss 在核验阶段指出 duty_reporter / quant_assistant 均使用下划线——这说明下划线是该目录层的既有惯例，正确方向应是 strategy-researcher 向下划线回归，而非反过来。

保留两层分离的共识：
- **目录层（文件系统）**：下划线 `strategy_researcher`
- **逻辑 ID 层（Charter agent-id / SKILL.md name）**：连字符 `strategy-researcher`

这两层不强行统一，分别服务不同关注点。

## 2. 任务

### Phase 1 — Windows 端
- `git mv openclaw_skills/strategy-researcher openclaw_skills/strategy_researcher`
- 全文搜索 `strategy-researcher` 作为**路径引用**时（不是作为 agent-id / name 字段时）的命中，逐项改回下划线

### Phase 2 — Ubuntu 端
- `mv ~/.openclaw/workspace/skills/strategy-researcher ~/.openclaw/workspace/skills/strategy_researcher`

### Phase 3 — 文档扫描
- `aos_repo` 全文搜索 `strategy-researcher`：
  - **路径/目录引用**：改回 `strategy_researcher`
  - **agent-id / SKILL.md name / 叙述性引用**：保留连字符不动
- 新旧工单（004b 已关闭）文本**不回改**，保留历史原貌

### Phase 4 — 自检
- Ubuntu 端 `python3 scripts/kb_query.py index` 返回 200
- Windows 端 SKILL.md / TOOLS.md / scripts 齐全

### Phase 5 — Commit
- commit message: `chore(skills): revert 004b — align strategy_researcher with existing underscore convention`
- worklog 追加到本工单

## 3. 验收

1. 全仓搜索**路径上下文**中的 `strategy-researcher` 无命中
2. Charter / SKILL.md 的 name 字段仍为 `strategy-researcher`（这是对的，别改）
3. Ubuntu 三个 skill 目录统一为下划线：`duty_reporter/`、`quant_assistant/`、`strategy_researcher/`
4. kb_query.py index 通过

## 4. 显式排除

- 不改 duty_reporter、quant_assistant（它们本来就对）
- 不改 Charter 的 agent-id（那是逻辑层，本就该用连字符）
- 不改任何脚本逻辑

## 5. 给我自己的教训（记录给后续所有工单做参考）

**在"归一命名"这类看似无害的基础设施工单里，必须先完整盘点当前命名现状，再决定归一方向**。本次错误的根源是我从"局部看到连字符"就跳到"全局应当连字符"，跳过了"另外两个 skill 实际长什么样"的核实步骤。

今后所有命名类工单开工前必须先做 5 分钟现状盘点，列出所有相关点位当前形态，再讨论归一方向。这条加入 openclaw-agent 的自我 checklist。

## worklog

### 2026-05-07 @openclaw-agent

- Phase 1 ✅ `git mv openclaw_skills/strategy-researcher → strategy_researcher`
- Phase 1 ✅ Fixed SKILL.md runtime_dir path reference
- Phase 3 ✅ Scanned aos_repo; fixed charter path references (runtime_ref, Skill path, archive path)
- Phase 4 ✅ SKILL.md / TOOLS.md / scripts 齐全
- Phase 5 ✅ committed: `chore(skills): revert 004b - align strategy_researcher with existing underscore convention`
- Ubuntu 侧 Phase 2（mv 目录）和 Phase 4（kb_query.py index）待执行
